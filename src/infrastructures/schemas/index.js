const userCollection = require('./user.schema');
const bookCollection = require('./book.schema');
const memberCollection = require('./member.schema');

module.exports = {
  userCollection,
  bookCollection,
  memberCollection,
};
